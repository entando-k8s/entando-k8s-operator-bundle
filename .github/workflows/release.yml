name: RELEASE

on:
  workflow_dispatch:
    inputs:
      param1:
        description: 'The first parameter'
        required: true
      param2:
        description: 'The second parameter'
        required: false

jobs:
  download-operator-bundle:
    name: Download entando-k8s-operator-bundle
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
          ref: develop
    - name: Run script
      run: |
        echo "Running script..."
        echo "Param1 value: ${{ github.event.inputs.param1 }}"
        echo "Param2 value: ${{ github.event.inputs.param2 }}"